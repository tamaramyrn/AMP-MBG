# AMP MBG Backend Environment
# Copy to .env and configure

# ===========================================
# DEVELOPMENT (default)
# Database: Docker PostgreSQL (make db-up)
# Storage: Local filesystem
# ===========================================

# ===========================================
# STAGING (Supabase free tier)
# Database: Supabase PostgreSQL
# Storage: Supabase Storage
# ===========================================

# ===========================================
# PRODUCTION (DigitalOcean + Cloudflare)
# Database: VPS PostgreSQL
# Storage: Cloudflare R2
# ===========================================

NODE_ENV=development
PORT=3000

# Database
DATABASE_URL=postgresql://ampmbg:ampmbg_dev_password@localhost:5432/ampmbg_dev

# JWT
JWT_SECRET=dev-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:5173

# Storage: local | supabase | r2
STORAGE_TYPE=local
LOCAL_UPLOAD_DIR=./uploads

# S3-Compatible Storage (staging/production)
S3_ENDPOINT=
S3_REGION=auto
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_BUCKET=
S3_PUBLIC_URL=



